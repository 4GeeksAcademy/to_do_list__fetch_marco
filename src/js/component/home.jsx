import React from "react";
import TodoList from "./todolist";

//create your first component
const Home = () => {
	// Input for username
	// Input to get new task
	// List to show all todo list tasks (all todos have one todo icon)



	return (
		<div className="container">
			<TodoList />
		</div>
	);
};

export default Home;
